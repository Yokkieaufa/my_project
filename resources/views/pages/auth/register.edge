@layout.app({ title: 'Register' })

<div class="relative min-h-screen">


  <!-- Content -->
  <div class="relative z-10 flex items-center justify-center min-h-screen px-4">
    <div class="w-full max-w-5xl mx-auto p-6">
      <div class="bg-white/80 backdrop-blur border border-gray-200 rounded-2xl shadow-lg flex flex-row gap-x-8 overflow-hidden">
        
        <!-- Gambar di kiri -->
        <div class="hidden md:block w-1/2">
          <img src="/image/friren.jpg" alt="Banner" class="w-100 h-full object-cover rounded-l-1xl" loading="lazy"  />
        </div>

        <!-- Form di kanan -->
        <div class="w-full md:w-1/2 p-6 sm:p-8">
          <div class="text-left mb-4">
            <h1 class="text-3xl font-bold text-green-800">🌱 Register</h1>
            <p class="mt-2 text-sm text-gray-600">
              Already have an account?
              <a class="text-blue-600 hover:underline" href="/login">Log in</a>
            </p>
          </div>

          <form action="/register" method="post" class="space-y-5">
            {{ csrfField() }}

            <div>
              <label for="name" class="block text-sm font-medium mb-1">Name</label>
              <input type="text" id="name" name="name" required value=""
                class="py-3 px-4 block w-full border border-gray-300 rounded-xl text-sm focus:ring-2 focus:ring-green-300">
              @inputError('name')
                <p class="text-xs text-red-600 mt-2">{{ $message }}</p>
              @end
            </div>

            <div>
              <label for="username" class="block text-sm font-medium mb-1">Username</label>
              <input type="text" id="username" name="username" required value=""
                class="py-3 px-4 block w-full border border-gray-300 rounded-xl text-sm focus:ring-2 focus:ring-green-300">
              @inputError('username')
                <p class="text-xs text-red-600 mt-2">{{ $message }}</p>
              @end
            </div>

            <div>
              <label for="email" class="block text-sm font-medium mb-1">Email</label>
              <input type="email" id="email" name="email" required value=""
                class="py-3 px-4 block w-full border border-gray-300 rounded-xl text-sm focus:ring-2 focus:ring-green-300">
              @inputError('email')
                <p class="text-xs text-red-600 mt-2">{{ $message }}</p>
              @end
            </div>

            <div>
              <label for="password" class="block text-sm font-medium mb-1">Password</label>
              <input type="password" id="password" name="password" required
                class="py-3 px-4 block w-full border border-gray-300 rounded-xl text-sm focus:ring-2 focus:ring-green-300">
              @inputError('password')
                <p class="text-xs text-red-600 mt-2">{{ $message }}</p>
              @end
            </div>

            <div>
              <label for="password_confirmation" class="block text-sm font-medium mb-1">Confirm password</label>
              <input type="password" id="password_confirmation" name="password_confirmation" required
                class="py-3 px-4 block w-full border border-gray-300 rounded-xl text-sm focus:ring-2 focus:ring-green-300">
            </div>

            <div class="flex justify-between items-center">
              <a href="/" class="text-sm text-gray-500 hover:text-gray-700 transition">
                ← Kembali ke Beranda
              </a>
              <button
                class="py-2.5 px-5 inline-flex items-center gap-x-2 text-sm font-semibold rounded-xl border border-transparent bg-green-600 text-white hover:bg-green-700 transition">
                Register
              </button>
            </div>
          </form>
        </div>
        
      </div>
    </div>
  </div>
</div>
@end
